<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fishtudo.awesomeseries">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AwesomeSeries">
        <activity
            android:name=".ui.activities.PeopleSearchActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.FavoriteListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.UnlockActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.CreateAPinActivity"
            android:exported="false"
            android:label="@string/activity_title_create_pin" />
        <activity
            android:name=".ui.activities.EpisodeDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ShowDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>